# Phase 11 — Failure Modes & Safety Boundaries

Purpose: classify meta-learning unsafe attention patterns via controlled breakage.

This phase introduces intentionally broken attention variants *in experiment code only*.